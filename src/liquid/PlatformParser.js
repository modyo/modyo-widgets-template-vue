export default class PlatformParser {
  // eslint-disable-next-line class-methods-use-this
  parse(liquidString) {
    return liquidString;
  }

  // eslint-disable-next-line class-methods-use-this
  parseAsync(liquidString) {
    return liquidString;
  }
}
